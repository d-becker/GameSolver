#include "FirstStage.hpp"

namespace gs {

TreeNode FirstStage::alpha_beta_pure(std::shared_ptr<GameState> game_state,
				     size_t depth,
				     eval_type alpha,
				     eval_type beta,
				     bool maximizing) const {
	// TODO
}
  
} // namespace gs
